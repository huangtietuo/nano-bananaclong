# 生成历史功能

## 功能概述

生成历史功能允许用户查看、管理和重用他们之前的AI生成内容。这个功能模仿了图片中展示的界面设计，提供了完整的历史记录管理体验。

## 主要特性

### 1. 历史记录展示
- **缩略图预览**: 显示生成内容的小图预览
- **提示词显示**: 展示用于生成的原始提示词
- **元数据信息**: 包括生成类型、使用模型、时间戳等
- **积分消耗**: 显示每次生成消耗的积分数量

### 2. 筛选和搜索
- **时间筛选**: 今天、本周、本月、全部时间
- **类型筛选**: 图像编辑、文本转图像、视频生成
- **关键词搜索**: 在提示词和模型名称中搜索
- **清除筛选**: 一键重置所有筛选条件

### 3. 交互功能
- **详情查看**: 点击眼睛图标查看完整的生成详情
- **提示词复制**: 复制提示词到剪贴板
- **内容下载**: 下载生成的图片或视频文件
- **历史删除**: 删除不需要的历史记录
- **重新使用**: 将历史记录的参数应用到当前生成器

### 4. 详情弹窗
- **大图预览**: 查看生成内容的完整尺寸
- **完整信息**: 显示所有生成参数和设置
- **一键应用**: 将历史记录的设置应用到当前生成器

## 技术实现

### 组件结构
```
components/
├── history-manager.tsx     # 主历史管理组件
├── ui/toast.tsx           # Toast通知组件
└── generator.tsx          # 更新后的生成器组件

hooks/
└── use-history.ts         # 历史记录管理Hook

app/api/
└── history/route.ts       # 历史记录API端点

supabase/migrations/
└── 20240127000001_create_generation_history.sql  # 数据库迁移
```

### 数据库设计
```sql
CREATE TABLE generation_history (
  id UUID PRIMARY KEY,
  user_id UUID REFERENCES auth.users(id),
  type TEXT CHECK (type IN ('image-edit', 'text-to-image', 'video-generation')),
  prompt TEXT NOT NULL,
  model TEXT NOT NULL,
  aspect_ratio TEXT,
  resolution TEXT,
  duration TEXT,
  output_urls JSONB,
  credits INTEGER,
  status TEXT DEFAULT 'completed',
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);
```

### API端点
- `GET /api/history` - 获取历史记录列表（支持筛选和搜索）
- `POST /api/history` - 保存新的历史记录
- `DELETE /api/history?id=xxx` - 删除指定历史记录

## 使用方法

### 1. 查看历史记录
1. 在生成器页面点击左侧导航的"历史"选项
2. 系统会自动加载用户的历史记录
3. 使用筛选器和搜索框来查找特定记录

### 2. 重用历史设置
1. 在历史记录列表中找到想要重用的记录
2. 点击眼睛图标打开详情弹窗
3. 点击"使用此提示词"按钮
4. 系统会自动切换到对应的生成模式并填入参数

### 3. 管理历史记录
- **复制提示词**: 点击复制图标将提示词复制到剪贴板
- **下载内容**: 点击下载图标保存生成的文件
- **删除记录**: 点击垃圾桶图标删除不需要的记录

## 安全特性

### 行级安全策略 (RLS)
- 用户只能访问自己的历史记录
- 所有数据库操作都经过用户身份验证
- 防止跨用户数据泄露

### 数据验证
- 服务端验证所有输入参数
- 防止SQL注入和XSS攻击
- 限制查询结果数量防止性能问题

## 性能优化

### 前端优化
- 使用React Hook进行状态管理
- 实现客户端筛选减少API调用
- 图片懒加载和错误处理

### 后端优化
- 数据库索引优化查询性能
- 分页加载大量历史记录
- 缓存常用查询结果

## 扩展功能

### 未来可能的增强
1. **批量操作**: 支持批量删除、下载历史记录
2. **收藏功能**: 标记重要的历史记录
3. **分享功能**: 分享生成结果给其他用户
4. **导出功能**: 导出历史记录为CSV或JSON格式
5. **统计分析**: 显示用户的生成统计和积分使用情况

## 故障排除

### 常见问题
1. **历史记录不显示**: 检查用户登录状态和数据库连接
2. **筛选不工作**: 确认API参数格式正确
3. **删除失败**: 检查用户权限和网络连接
4. **图片不显示**: 验证图片URL的有效性

### 调试方法
- 检查浏览器控制台的错误信息
- 查看网络请求的响应状态
- 验证数据库中的数据完整性